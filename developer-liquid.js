/* DroidX Developer Liquid — subtle water response for developer card */
(()=>{const boot=()=>{const card=document.querySelector('.developer-liquid');if(!card||card.dataset.ready)return;card.dataset.ready='1';card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect();const x=((e.clientX-r.left)/r.width)*100;const y=((e.clientY-r.top)/r.height)*100;card.style.setProperty('--mx',x+'%');card.style.setProperty('--my',y+'%');card.style.setProperty('--water-x',((x-50)/50*7).toFixed(2)+'px');card.style.setProperty('--water-y',((y-50)/50*5).toFixed(2)+'px')},{passive:true});card.addEventListener('pointerdown',()=>card.classList.add('developer-liquid-pressed'));const up=()=>card.classList.remove('developer-liquid-pressed');card.addEventListener('pointerup',up);card.addEventListener('pointercancel',up);card.addEventListener('pointerleave',up)};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot()})();