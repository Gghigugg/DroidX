/* DroidX Liquid Material — lightweight touch depth */
(()=>{const boot=()=>{document.querySelectorAll('.glass,.card,.primary,.secondary,.command-trigger,.nav-item,.search-box').forEach(el=>{if(el.dataset.liquidMaterial)return;el.dataset.liquidMaterial='1';el.addEventListener('pointerdown',()=>el.classList.add('liquid-pressed'),{passive:true});const release=()=>el.classList.remove('liquid-pressed');el.addEventListener('pointerup',release,{passive:true});el.addEventListener('pointercancel',release,{passive:true});el.addEventListener('pointerleave',release,{passive:true})})};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot()})();
