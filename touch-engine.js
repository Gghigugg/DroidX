(()=>{const init=()=>{const reduced=window.matchMedia&&window.matchMedia('(prefers-reduced-motion: reduce)').matches;const fine=window.matchMedia&&window.matchMedia('(pointer:fine)').matches;const targets=()=>document.querySelectorAll('.primary,.secondary,.command-trigger,.nav-item,.card,.favorite-btn');const ripple=(el,e)=>{if(reduced||el.classList.contains('favorite-btn'))return;const r=el.getBoundingClientRect(),x=(e.clientX||r.left+r.width/2)-r.left,y=(e.clientY||r.top+r.height/2)-r.top,s=Math.max(r.width,r.height)*1.7;const q=document.createElement('span');q.className='touch-ripple';q.style.left=x+'px';q.style.top=y+'px';q.style.width=q.style.height=s+'px';el.appendChild(q);setTimeout(()=>q.remove(),650)};const bind=()=>targets().forEach(el=>{if(el.dataset.touchEngine)return;el.dataset.touchEngine='1';el.addEventListener('pointerdown',e=>{if(e.pointerType==='mouse'&&fine===false)return;el.classList.add('touch-press');ripple(el,e)},{passive:true});['pointerup','pointercancel','pointerleave'].forEach(t=>el.addEventListener(t,()=>el.classList.remove('touch-press'),{passive:true}))});bind();const obs=new MutationObserver(bind);obs.observe(document.body,{childList:true,subtree:true});if(!reduced&&fine){document.addEventListener('pointermove',e=>{const el=e.target.closest('.card,.hero,.dashboard,.search-box,.topbar,.info,.command-dialog');if(!el)return;const r=el.getBoundingClientRect();el.style.setProperty('--tx',(((e.clientX-r.left)/r.width-.5)*2).toFixed(3));el.style.setProperty('--ty',(((e.clientY-r.top)/r.height-.5)*2).toFixed(3))},{passive:true})}};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init()})();