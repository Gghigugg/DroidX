(()=>{const boot=()=>{const splash=document.getElementById('splashScreen');if(!splash)return;document.body.classList.add('splash-lock');const reduced=window.matchMedia?.('(prefers-reduced-motion: reduce)').matches;const progress=splash.querySelector('.splash-progress span');const percent=splash.querySelector('.splash-percent');const status=splash.querySelector('.splash-status');const messages=[['1','Waking DroidX…'],['18','Building the glass surface…'],['36','Preparing Android shortcuts…'],['54','Polishing the liquid interface…'],['72','Loading quick tools…'],['88','Syncing your workspace…'],['96','Almost ready…'],['100','DroidX is ready ✓']];let value=1;let start=performance.now();const render=()=>{if(progress)progress.style.width=value+'%';if(percent)percent.textContent=value+'%';let m=messages.findLast?messages.findLast(x=>value>=Number(x[0])):messages.slice().reverse().find(x=>value>=Number(x[0]));if(status&&m)status.textContent=m[1]};render();const finish=()=>{value=100;render();setTimeout(()=>{splash.classList.add('splash-hide');document.body.classList.remove('splash-lock');splash.setAttribute('aria-hidden','true');setTimeout(()=>splash.remove(),460)},180)};if(reduced){finish();return}const tick=()=>{const elapsed=performance.now()-start;const target=Math.min(100,Math.floor(1+elapsed/23));if(target>value){value=target;render()}if(value<100){requestAnimationFrame(tick)}else finish()};requestAnimationFrame(tick)};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot()})();